import { DaVarDirective } from './da-var.directive';

describe('DaVarDirective', () => {
  it('should create an instance', () => {
    const directive = new DaVarDirective();
    expect(directive).toBeTruthy();
  });
});
